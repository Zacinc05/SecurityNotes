

| Use | Command | Usage | Flags / Description |
| :---- | :---- | :---- | :---- |
| Help / Documentation | man– help | command –helpman command |  |
| login | sshsftp | ssh username@remote\_host(ssh user@ip\_address) |  |
| Globbing wildcardsReplace \* argument with any files that match pattern | \* (mult char)? (1 char) | ex) echo Look: file\_\*(return file\_a, file\_b if exist, else ‘file\_\*’)ex) file\_\* can be file\_abs or file\_a, etc.ex) file\_? Can be file\_a, file\_b, etc.ex) ls when ls command unavailable$ echo \* | Searching for something like file\_\* will return file\_\* if nothing found |
| Globbing  \[\]Limited form of ? | \[\] | command name\_\[ab\]uses name\_a or name\_b | Single char search. Pick a range of char for ? |
| Globbing \!Exclusionary glob | \! | Command name\_\[\!ab\]Uses anything for x except a and b | Invert search of \[\]. Filter out specific ? |
| Variable Access | $ | Command $VARex) echo $PWD (print cwd) | Specify ur searching for a variable.$x can also be used for the x-th argument in a script |
| Variable SetVariable Store Output | VAR= | VAR=””, VAR=(quote for multi word)VAR=$(command)ex) FLAG=$(cat /flag)(save output to var) | Way to change variable value.Use a $ symbol for the value to be a command output |
| Variable Export | export | export VARex) export and view in sh (minimal shell)VAR=1337export VARsh$ echo $VAR | Mark a shell variable so that it becomes an environment variable (passes to child processes) |
| Variable Export Print | env | env (in terminal reveals all)ex) env | grep “key” (search for key) | Print every exported variable set in shell |
| Variable Reading Input | read | read VARex) show prompt before reading input read \-p “Prompt text: “ VARex) read HOLD \< /flag && echo "$HOLD"(set variable to program output, print)ex) setup read, give val to var | \-p (specify prompt, has no effect)Used as an input. Entering read VAR will then prompt the user for var input (unless rest of command gives an input).Same role in scripts. |
| Print message | echo | echo “hi” (hi in terminal)ex) print variableecho $PWD (print cwd)ex) feed command to script in terminalecho “cat /flag” \> shell.sh | Good for confirming an execution |
| Read lines with string | grep | grep “string” /pathex) all file names that have ‘500’ in the file textgrep \-l "500" \*ex) all matches (w/o name/path) under "a.log" that contain the string "500".$ find . \-name "a.log\*" | grep \-rh "500"$ grep \-r \-h "500" \--include "a.log\*"ex) live output/program | grep “string” ex) grep errors/program 2\>& 1 | grep “string”(pipe combined stderr and stdout as normal, able to search contents)ex) \-v invert/program | grep \-v “string” | \-v (invert)\-r \-recursive (recursive)\-l list file w/ match\--include FILE\_PATTERN(for specific file name)\-h (remove file name prefix)Given a set of contents, will check each line for a specific pattern.If pattern found, the entire line is output. |
| Read file | cat | Cat (file/item) | Read contents of a fileCat with no arguments will cat inputs to stdout |
| Character count | wc | wc \[OPTIONS\] \[FILE...\] | \-l lines, \-w words, \-c bytes,\-m characters, \-L print longest line  |
| unique | uniq | ex) uniq file (remove consecutive dupes)ex) sort data.txt | uniq (rem duplicates) | \-c occurrence count for each line\-d only duplicates\-u unique only |
| Text processer | awk | awk \[options\] 'pattern {action}' input-file \> output-fileex) same as cat$ awk '{print}' file.txtex) print column 1 & 4\. separate=spacesawk \-F' ' '{print $1, $4}' file.txtex) run with input scriptawk \-f input.awk file.txt(script: { print $1, "has salary", $4 }) | \-F custom field separator \-f Reads awk program from a file \-v Assigns a variable before exetext-processing and pattern-scanning tool in Linux used for manipulating data and generating formatted reports |
| Write | nano | nano /file\_path | Recall editing a .bashrc file to mess with a user’s login process |
| Read reverse | rev | ex) echo hi | rev ex) echo hi \> \>(rev)(both print ih to terminal) | Output is reverse. |
| Pattern Substitute | sed | sed “s/oldword/newword/g”ex) remove fake content/program | sed “s/string//g” | Given contents, will swap a pattern and output. |
| Compare files | diff | diff file1 file2ex) program output differencesDiff \<(/prog\_a) \<(/prog\_b) | Compares 2 sets of content,Output only the unique lines. |
| Print working directory | pwd | pwd | Sometimes referred as ‘cwd’(current working directory) |
| Change directory | cd | cd /path | Relative paths: ‘.’ & ‘..’ & ‘./’Absolute paths: ‘/’ |
| Listing files | lsls \-l | ls /pathls \-lls \-l template:—------- \# owner owner id Month day time file\_name ex)\-rw-r--r-- 1 hacker hacker 0 May 22 13:42 college\_fileUser can read/write. Group can read. Others can read. File owned by hacker. File is “college\_file”, modified 5/22. File id 0\. | \-a (include hidden)\-l (details)List content.For \-l, gives a descriptive list of permissions. Can also specify a specific file (ls \-l /path).Separates permissions on a list by 10 dashes. First char is d for directoryor \- for file.Next 9 char are access perm.\- 3 for user perm.\- 3 for group perm\- 3 for other permThere are 2 columns showing user owns the file |
| history | historyCtrl+r | history |  |
| create | touch | Touch /path | Create empty file if doesn't existWill also update modification time.\-a: Change only access time  \-m: Change only modification time \-c or \--no-create: Does not make if it does not already exist. \-r reference\_file : Use timestamps of reference\_file \-t TIMESTAMP: Manually set access and modification times |
| remove | rmrm dir | rm /pathex) rm \-r dir\_a (recursive remove)note) “rm \-rf \*” wont get all subdirectories & contents. “find \-delete” for hidden. | \-r (recursive)\-i (interactive, ask for confirmation)\-f (force, bypass write protected)\-v (verbose, show detail as delete)\-I (prompt once for multiple rm file)\--no-preserve-root (override) |
| move | mv | mv /original\_path /new\_path | Original\_path gone\-i (interactive, ask for overwrite) \-f (force, override existing files) \-v (verbose, show detail as move) \-u (update, weird rule w/ overwrite) |
| copy | cp | cp /original\_path /new\_path | Original\_path remains |
| Make directory | mkdir | mkdir /path |  |
| Find file.Matches every file | find | find /pathfind / \-name file\_name (start root)ex) remove all .doc files from cwdfind . \-type f \-name '\*.doc' \-deleteex) Print the relative file paths that start with a.logfind \-name "a.log\*" ex) delete everything from relative path, hidden toofind \-deleteex) $ find my\_dir/my\_subdirmy\_dir/my\_subdirmy\_dir/my\_subdir/my\_subfileex) all matches (w/o name/path) under "a.log" that contain the string "500". $ find . \-name "a.log\*" | grep \-rh "500" | \-name (search file name) |
| Find executable | which | which \[-a\] filenameex)$ which cat /run/dojo/bin/cat | which is used to determine the location of executable programs. It searches through directories listed in the $PATH variable |
| Links.Create hard/soft link | ln | ln /path /hard\_link\_pathln \-s /path /soft\_link\_path ex) soft link of a trusted file$ rm /tmp/collab/evil-commands.txt $ ln \-s /home/zardus/.bashrc /tmp/collab/evil-commands.txt (writing to it will now affect startup) | \-s (soft link) |
| Translate Charactersx char in og string becomes x char in new\_string | tr | tr og\_string new\_stringex) echo “test” | tr te ba    (output basb)ex) echo “test” | tr t ba      (output besb)ex) echo “test” | tr ts a      (output aeaa)ex) echo PAWN | tr \-d A   (output PWN)ex) make newlineecho “hello\_world\!” | tr \_ “\\n” | \-d delete characters |
| Chaining CommandChain on Success | ; (semicolon)&& | ex) echo VAL \> VAR; cat VARex) /program\_a ; /program\_bcommand\_1 && command\_2 |  |
| Chain on Failure(run only if prev fails) | || | command1 || command2 |  |
| Extract first/last lines(default at 10\) | head / tail | ex) cat /file | head (print first 10 lines)ex) cat /file | head \-n 2 (print top 2 lines)ex) take range of rows (3-7)Head \-n 7 /path | tail \-n 5ex) tail \-5 access.log (basic tail) | \-n (number of lines) |
| Extract specific sections of text | cut | cut OPTION \[FILE\]ex) first columnCut \-d ‘ ‘ \-f 1 file\_pathex) column range (2-3)Cut \-d ‘ ‘ \-f 2-3 file\_path | \-d (column delimiter)\-f (field number, i.e. which column) |
| Sorting Data | sort | sort /path | \-r (reverse)\-n (numeric sort)\-u (unique)\-R (random) |
| Redirect output.Stdout to files | \> | (action) \> fileex)Echo hi \> asdf | cat asdf (output hi)Echo hi 1\> asdf (specify standard input)  | 0\> standard input1\> standard output2\> standard error |
| Append output.Add not replace | \>\> | (action) \>\> file | 0\> standard input1\> standard output2\> standard error |
| Redirect error.Filter out command data that may produce data via standard error. | 0\>1\>2\> | ex) command \> out.log 2\> err.log(redirect output to out, errors to err) | 0\> standard input1\> standard output2\> standard error |
| Redirect input. | \< | Receive \< messageex) echo VAL \> Hold | /program \< Hold(give value to hold file, redirect input to program) |  |
| Redirect output/input/error |  |  | File Descriptor(communication channel) valuesFD 0: Standard inputFD 1: Standard OutputFD 2: Standard Error |
| Redirect FD to FD (File Descriptor) | \>& | (FD val)\>& (FD val)ex) grep errors./program 2\>& 1 | grep “string”(pipe combined stderr and stdout as normal, able to search contents) | File Descriptor (communication channel) values FD 0: Standard input FD 1: Standard Output FD 2: Standard Error |
| Duplicating piped data to and \# of filesWrite to multiple filesWrite to multiple prog | tee | (previous command) | tee pathex) echo hi | tee /path\_a /path\_bex) duplicate data to 2 filesecho hi | tee file\_a \> file\_bex) duplicate to 2 programs/program | tee \>(/prog\_a) \>(/prog\_b)ex) duplicate data to a file and commandecho hi | tee file | cat(cat the message and store in file)ex) echo hi | tee \>(rev)(print out forwards and backwards)ex) read data while also piping it/program | tee hold\_file | /program2(pipe program to program 2, copy to hold\_file) |  |
| Process Substitution.Hook input/output of programs to arguments as commands | \<(command) | ex) program output differencesDiff \<(/prog\_a) \<(/prog\_b)ex) write to multiple files\- echo “hi” | tee \>(cat \>file1) \>(cat \>file2)\- echo "hello world" \> \>(tee file1 file2)ex) split pipe stderr and stdout\- /prog 2\> \>(/prog\_stderr) | /prog\_stdout\- /prog \> \>(/prg\_stdout) 2\> \>(/prg\_stderr)V1: pipe directly to stdout, also pass error to stderr by treating prog as arg.V2: pass both program as arg so output can be redirected |  |
| Process Listing(process snapshot,Process status) | ps | “ps \-ef”    or    “ps aux” | \-e (every process),  \-f (full format)a (all),  u (user readable)x (not running in terminal) |
| Process Kill | kill | kill (process ID (pid))ex) find sleeping task (pid \= 342), kill itps \-e | grep sleepkill 342 |  |
| Process InterruptProcess SuspendProcess ResumeLeave screen session | ctrl+cctrl+zfgctrl-d / exit |  |  |
| Process Background Foreground | bgfg | bg \= &ex) fork bomb\#\!/bin/bash /path/fork.sh & /path/fork.sh & |  |
| Create virtual terminal inside terminal  | screen |  |  |
| Screen CommandsExit virtual TerminalDetachAttatchWindow Commands | exitscreen crtl  | ctrl-A, followed by dscreen \-rscreen \-r pid      (pid for specific screen)screen \-ls          (get pid/name for \-r) | d (detatch)\-r (reattatch)screen \-ls (list screens)Ctrl-A c \- Create new window Ctrl-A n \- Next window Ctrl-A p \- Previous window Ctrl-A 0-9 \- Jump to window 0-9 Ctrl-A " \- bring up selection menu  |
| Terminal MultiplexerTmux (like screen) | tmux | Crtl-b, followed by d tmux \-ls               (list sessions) tmux attach        (attach all sessions)  | Ctrl-B c \- Create new window Ctrl-B n \- Next window Ctrl-B p \- Previous window Ctrl-B 0-9 \- Jump to window 0-9 Ctrl-B w \- Nice window picker |
| Process Exit CodeCode when finish running and terminate | $? | ex) finding a code of file\_atouch /file\_aecho $?Typically return 0 for a successful command. 1 or something else if permission denied or failure |  |
| User Login | su | ex) root loginsu (will then likely have to provide pass)ex) user loginsu some-user (prob prompt password) |  |
| User Check | whoami | whoami (list current user) |  |
| bashRun script, passing as arg to a new shell instance(read file commands) | bash | bash file.sh (sh is part of name already)bash file.sh arg1 arg2 arg3ex) create shell to run 2 filesecho "/prog\_a && /prog\_b" \> x.shbash x.shex) pipe from scriptbash x.sh | /challenge/solve ex) run without bashecho "/prog" \> x.sh chmod \+x x.sh /x.sh | $1 \= arg1, $2…etc.Use $x as placeholder for x arg.ex) echo “$1 $2” in the bash script prints the first 2 args |
| Scripting with Conditionals | if / else /elif / fi\==\[ \] | ex) script if statementif \[ "$1" \== "skibidi" \] then     echo "toilet"elif \[“$1” \== “ohio”\]then    echo “rizz”else    echo “baka” fi |  |
| Check groups you’re a part of | idgroups | ex) hacker@dojo:\~$ id uid=1000(hacker) gid=1000(hacker) groups=1000(hacker) |  |
| Change group ownership | chgrp  | chgrp user file |  |
| Change mode/permissions | chmod | chmod \[OPTIONS\] MODE FILEchmod u+s \[program\]chmod u-r,o-r /progex)u+r, add read access to user's perm g+wx, add write & exe access to group o-w remove write access for others a-rwx remove all for user/group/world | r \- can read the file/list/directory w \- can modify the file/list/directory x \- can execute file as a prog, or cd if a directory \- \- nothing t \- allows the owners of files to rename or remove files in the directory u \= user, g \= group,o \= others, a \= alls \= executable with SUID\=     set permissions\=-    remove |
| shebang | \#\! | c\#\!/bin/bash for bash scripts \#\!/usr/bin/python3 for Python scripts \#\!/bin/sh for POSIX shell scripts |  |
| Path navigation.PATH stores a list of directories to find commands. | PATH= | PATH=”/path” (set however)ex) make custom script easier to access$ PATH=/home/hacker/scripts $ goodscript \*goodscript runs now\*ex) break path (i.e. smtn like rm)PATH=”” ex) add a win script that uses cat for flag$ $PATH$ echo "/run/dojo/bin/cat /flag" \> win$ Path=”/win\_path:(rest of path after ‘:’)”ex) hijack rm to print /flag$ echo “cat /flag” \> rm$ PATH=”/path\_to\_fake\_rm:(cont\_path)”(use of rm will just run fake rm) |  |
| Find command | which | which commandex) find cat file location$ which cat/bin/cat |  |
| Infinite output | yes | yes ex) print 10 ‘y’ lines (unless specify x)yes | headex) fill disk space$ yes \> buffer.txt yes: standard output: Disk quota exceeded $ /check  Well done, you clogged the disk.  | outputs lines containing ‘y’ foreverEasy to fill disk space |

